/* MẪU. Chép thành arduino_secrets.h rồi điền giá trị thật.
   arduino_secrets.h nằm trong .gitignore — KHÔNG BAO GIỜ commit.
   Giá trị phải khớp với planb_cloud/.env (MQTT_USER / MQTT_PASSWORD). */
#pragma once
#define SECRET_MQTT_USER "hutieu"
#define SECRET_MQTT_PASS "dien-mat-khau-vao-day"
