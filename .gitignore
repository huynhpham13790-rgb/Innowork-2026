# ---- Secret: TUYỆT ĐỐI không commit ----
.env
*.env
!.env.example
VEDCaPhenika/planb_cloud/mosquitto/passwd
VEDCaPhenika/planb_cloud/mosquitto/passwd.*

# ---- File nén / bản sao lưu ----
*.rar
*.zip
*.7z

# ---- File tạm của LibreOffice / Office ----
.~lock.*#
~$*
*.tmp
*.xlsx.tmp
_probe.txt

# ---- Build artifacts ----
build/
*.bin
*.elf
*.hex

# ---- Hệ điều hành / editor ----
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Cấu hình compose riêng của từng máy (port, IP LAN...)
docker-compose.override.yml

# ---- AI ----
# Dữ liệu thô (1,6 GB) và venv (2,6 GB): tải/dựng lại được bằng script.
ai/.venv/
ai/data/
# NHƯNG ai/models/ thì PHẢI commit: chỉ 88 KB, và firmware cần
# cell_ae_model.h để biên dịch. Không có nó thì repo không build được.
__pycache__/

# Dữ liệu NASA (210 MB) — tải lại bằng lệnh trong ai/README.md
ai/data/nasa_raw/
ai/data/nasa_battery.zip
ai/data/nasa_cycles.csv

# file tạm của Claude Code, không phải tài sản dự án
.claude/settings.local.json*
